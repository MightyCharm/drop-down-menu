import "./styles.css";
import { DropdownManager } from "./dropdown.js";

document.addEventListener("DOMContentLoaded", () => {
  const dropdownManager = new DropdownManager();
  dropdownManager.addFunctionality();
});
